{
  "name": "snowball-stats",
  "version": "3.0.2",
  "description": "Tracking Snowball statistics.",
  "scripts": {
    "all": "npm run snob basic && npm run axial basic && npm run dist && npm run nfts && npm run pools && npm run gasStats && npm run snob && npm run axial && npm run teddy && npm run axialDistCheck && npm run revenue",
    "quick": "npm run snob && npm run axial && npm run teddy && npm run dist && npm run revenue",
    "snob": "node scripts/snob.js",
    "nfts": "node scripts/nfts.js",
    "dist": "node scripts/dist.js",
    "axial": "node scripts/axial.js",
    "pools": "node scripts/pools.js",
    "axialDistCheck": "node scripts/axialDistCheck.js",
    "gasStats": "node scripts/gasStats.js",
    "teddy": "node scripts/teddy.js",
    "revenue": "node scripts/revenue.js"
  },
  "author": "Ncookie",
  "dependencies": {
    "axios": "^0.23.0",
    "ethers": "^5.5.1"
  }
}
